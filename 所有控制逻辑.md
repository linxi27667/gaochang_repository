# 高昌举升机控制逻辑总览

本文按当前五个项目源码重新整理控制逻辑：四个 STM32F407 举升机固件和一个 Web 管理端。固件均使用 FreeRTOS、EasyLogger/RTT 日志、TAS-LTE-892D_s4 4G DTU 通过 MQTT 接入 Web 平台。

更新日期：2026-07-10

## 1. 公共规则

### 1.1 安全优先级

所有固件都遵循“先安全、后动作、最后输出”的原则：

1. 急停、远程锁定、光电报警、非法按键组合、故障状态优先级最高。
2. 任一安全保护触发时，运动输出立即关闭。
3. 补油不能绕过急停、光电、远程锁定、故障和非法组合。
4. 物联网上报统一限速为约 1 秒 1 包：周期 telemetry、运动中高度包、事件包都使用 1000ms 间隔。

### 1.2 补油规则

补油只在带补油按钮的剪式举升机中有效：

```text
补油有效条件：
  上升按钮按下
  补油按钮按下
  下降按钮未按下
  锁定按钮未按下

补油按钮单独按下：
  不启动任何动作

补油与普通上升的主要差异：
  普通上升受上限位限制
  补油忽略上限位
```

## 2. 大剪固件 `GC_Big_Scissor`

### 2.1 项目角色

大剪支持主机/子机角色，旋转开关选择当前角色。角色切换时立即关闭所有输出并回到空闲。

### 2.2 输入输出

输入：

| 功能 | 引脚 | 有效电平 |
|---|---|---|
| 上升 | PG15 | 低有效 |
| 下降 | PE0 | 低有效 |
| 锁定 | PE1 | 低有效 |
| 急停 | PE2 | 低有效 |
| 补油 | PE3 | 低有效 |
| 旋转开关 | IN13 | 高=主机，低=子机 |
| 主机上限位 | PE5 | 高有效 |
| 主机下限位 | PE6 | 高有效 |
| 子机上限位 | PE7 | 高有效 |
| 光电 | PE8 | 高有效 |

输出：

| 功能 | 引脚 | 有效电平 |
|---|---|---|
| 电机 | PF8 | 高有效 |
| 下降阀 | PF9 | 高有效 |
| 主机气阀 | PD8 | 高有效 |
| 主机工作阀 | PD9 | 高有效 |
| 子机气阀 | PD10 | 高有效 |
| 子机工作阀 | PD11 | 高有效 |

### 2.3 动作逻辑

上升：

```text
空闲 + 上升按下 + 当前角色上限位未触发：
  电机 ON
  当前角色工作阀 ON
  延时 motor_to_valve_delay_ms 后当前角色气阀 ON

停止条件：
  上升按钮释放
  当前角色上限位触发
  安全保护触发
```

下降：

```text
空闲 + 下降按下：
  电机 ON
  当前角色工作阀 ON
  延时 motor_to_valve_delay_ms 后当前角色气阀 ON
  再保持当前角色 motor_hold_ms
  电机 OFF
  下降阀 ON
  当前角色工作阀/气阀保持 ON

主机下限位触发：
  立即全停回空闲

下降按钮释放：
  全停回空闲
```

锁定：

```text
空闲 + 锁定按下：
  当前角色工作阀 ON
  下降阀 ON
  电机 OFF
  气阀 OFF

锁定按钮释放：
  全停回空闲
```

补油：

```text
空闲 + 上升按下 + 补油按下：
  电机 ON
  当前角色工作阀 ON
  延时 motor_to_valve_delay_ms 后当前角色气阀 ON
  不检查当前角色上限位

任一按钮释放：
  全停回空闲
```

## 3. 两柱固件 `GC-Two_Pillars`

### 3.1 项目角色

该工程当前用于两柱举升机，同时保留多产品平台代码。两柱模式无补油、无光电。

### 3.2 输入输出

输入：

| 功能 | 引脚 | 有效电平 |
|---|---|---|
| 上升 | PG15 | 低有效 |
| 下降 | PE0 | 低有效 |
| 锁定 | PE1 | 低有效 |
| 急停 | PE2 | 低有效 |
| 上限位 | PE5 | 高有效 |

输出：

| 功能 | 引脚 | 有效电平 |
|---|---|---|
| 电机 | PF8 | 高有效 |
| 电磁铁 | PF9 | 高有效 |
| 下降阀 | PD8 | 高有效 |

### 3.3 动作逻辑

上升：

```text
空闲 + 上升按下 + 上限位未触发：
  电机 ON

停止条件：
  上升按钮释放
  上限位触发
```

下降：

```text
空闲 + 下降按下：
  电机 ON
  200ms 后电磁铁 ON
  电机累计运行到 2000ms 后：
    电机 OFF
    下降阀 ON
    电磁铁保持 ON

如果下降过程中上限位触发或锁定按钮按下：
  电机 OFF
  电磁铁 ON
  进入强制解锁等待
  到 2000ms 后下降阀 ON

下降按钮释放：
  全停回空闲
```

锁定：

```text
空闲 + 锁定按下：
  下降阀 ON
  电机 OFF
  电磁铁 OFF

锁定按钮释放：
  全停回空闲
```

## 4. 小剪固件 `GC_Small_Scissor`

### 4.1 输入输出

输入由 `app_io_map.c` 和 `app_lift_core.c` 共同定义：

| 功能 | 逻辑名 | 有效电平 |
|---|---|---|
| 上升 | up | 低有效 |
| 下降 | down | 低有效 |
| 锁定 | lock | 低有效 |
| 急停 | estop | 低有效 |
| 上限位 | upper | 高有效 |
| 补油 | refill | 低有效 |
| 光电 | photo | 高有效 |
| 下限位 | lower | 高有效，同时用于屏蔽光电 |

输出：

| 功能 | 引脚 | 有效电平 |
|---|---|---|
| 电机 | PF8 | 高有效 |
| 下降阀 | PF9 | 高有效 |
| 气阀 | PD8 | 高有效 |

### 4.2 状态机

小剪使用集中式状态机：采集输入、前置安全检查、状态切换、输出仲裁、最终安全闸门、硬件输出。

上升：

```text
空闲 + 上升按下 + 上限位未触发：
  电机 ON
  200ms 后气阀 ON
  下降阀 OFF

上升按钮释放或上限位触发：
  全停回空闲
```

下降：

```text
空闲 + 下降按下：
  down_prepare: 电机 ON
  200ms 后进入 down_hold_motor: 电机 ON + 气阀 ON
  气阀打开后保持 3000ms
  dropping: 电机 OFF + 下降阀 ON + 气阀 ON

上限位触发或锁定按钮按下：
  直接进入 dropping

下降按钮释放：
  全停回空闲
```

锁定：

```text
空闲 + 锁定按下：
  下降阀 ON
  气阀 ON
  电机 OFF

锁定按钮释放：
  全停回空闲
```

补油：

```text
空闲 + 上升按下 + 补油按下：
  电机 ON
  200ms 后气阀 ON
  下降阀 OFF
  不检查上限位

上升/补油任一释放，或下降/锁定介入：
  全停回空闲
```

安全：

```text
远程锁定：
  运动中转 safe_stop，输出全关

急停：
  输出全关，进入 estop；急停释放且按钮释放后回空闲

光电：
  下限位触发时屏蔽光电
  运动中触发光电需要远程清报警
  非运动触发光电可在光电恢复且按钮释放后自动清除

上升+下降同时按下：
  进入 safe_stop，输出全关

超时：
  上升/下降超过 60s 进入 fault
```

## 5. 超薄小剪固件 `GC_Thin_Scissor`

### 5.1 输入输出

输入：

| 功能 | 有效电平 |
|---|---|
| 上升 | 低有效 |
| 下降 | 低有效 |
| 锁定 | 低有效 |
| 急停 | 低有效 |
| 上限位 | 高有效 |
| 下限位 | 高有效 |
| 补油 | 低有效 |
| 光电 | 高有效 |

输出：

| 功能 | 引脚 | 有效电平 |
|---|---|---|
| 电机 | PF8 | 高有效 |
| 下降阀 | PF9 | 高有效 |
| 气阀 | PD8 | 高有效 |

### 5.2 动作逻辑

上升：

```text
空闲 + 上升按下 + 上限位未触发：
  电机 ON
  下降阀 OFF
  气阀 OFF

上升按钮释放或上限位触发：
  全停回空闲
```

下降：

```text
空闲 + 下降按下：
  电机 ON

达到 motor_to_valve_delay_ms：
  气阀 ON

达到 motor_hold_ms：
  电机 OFF
  下降阀 ON

上限位触发或锁定按钮按下：
  电机 OFF
  下降阀 ON
  气阀 ON

下限位触发：
  电机 OFF
  下降阀 ON
  气阀 OFF

下降按钮释放：
  全停回空闲
```

锁定：

```text
空闲 + 锁定按下：
  电机 OFF
  下降阀 ON
  气阀 OFF

锁定按钮释放：
  全停回空闲
```

补油：

```text
空闲 + 上升按下 + 补油按下：
  电机 ON
  下降阀 OFF
  气阀 OFF
  不检查上限位

上升/补油任一释放，或下降/锁定介入：
  全停回空闲
```

安全：

```text
上升+下降同时按下：
  如果正在动作则全停；空闲时保持输出全关

急停、光电、远程锁定：
  由 lift_core 公共安全层处理，触发时输出全关

下限位：
  下降时强制气阀 OFF，避免反挺
  锁定时气阀始终 OFF
  静止落地时屏蔽光电的正常遮挡；运动中光电始终触发全停
```

## 6. Web 管理端 `Gaochang_Iot_Web`

Web 端负责 MQTT 接入、设备管理、命令下发、报警/维修/日志/统计展示。

默认 MQTT：

```text
Broker: mqtt://mqtt.gclift.net:1883
Topic prefix: gaochang/lift
Telemetry: gaochang/lift/f407zet6/telemetry
Command: gaochang/lift/f407zet6/command
Status: gaochang/lift/f407zet6/status
```

主要命令：

| 命令 | 作用 |
|---|---|
| lock / unlock | 远程锁机 / 解锁 |
| get_status | 查询设备状态 |
| clear_alarm | 清除报警 |
| rename | 修改设备名称 |
| set_config / get_config | 下发/读取参数 |
| set_product_type | 切换产品类型 |
| buzzer_on / buzzer_off | 控制蜂鸣器 |

## 7. 文件与调试约定

1. `APP/Src` 放产品状态机、IoT JSON、DTU 协议、存储和业务逻辑。
2. `Driver/Src` 放 FreeRTOS 任务封装、周期调度和硬件驱动门面。
3. `Core/Src`、`BSP`、`Drivers`、`Middlewares` 保留 CubeMX/HAL/FreeRTOS/BSP 代码。
4. `RTT_easylog` 是日志库源码，不是运行日志，不应删除。
5. `*.log`、`rtt_capture*.txt`、`rtt_logger*.txt`、`jlink_*.txt` 属于临时构建/调试输出，可以清理。
